{
  "name": "moneyapp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/dev-server.mjs",
    "check": "node --check app.js && node --check supabase-config.js && node --check supabase-sync.js && node --check sw.js && node --check scripts/dev-server.mjs"
  }
}
